# 三角洲口琴自动演奏器（学习项目）

这是一个用 Python 编写的口琴曲谱播放器。当前版本读取简单的数字曲谱，计算每个音符需要使用的键盘按键、鼠标修饰键和持续时间，并在终端中预览演奏计划。

> 当前版本不会向游戏发送按键，也不会修改、读取或注入游戏进程。游戏对第三方自动化程序可能有处罚，请在添加真实输入功能前确认官方规则并自行评估账号风险。本项目不会实现反作弊绕过。

## 当前目标

- 读取文本曲谱。
- 支持普通、降调、半音和升调。
- 根据 BPM（每分钟拍数）计算按键持续时间。
- 在终端中安全预览演奏动作。

## 键位规则

| 曲谱音符 | 游戏显示 | 键盘按键 |
| --- | --- | --- |
| `1`–`7` | `1`–`7` | `Z X C V B N M` |
| `8` | 高音 `1` | `,` |

| 曲谱修饰词 | 鼠标动作 |
| --- | --- |
| `normal` | 不按鼠标 |
| `down` | 按住鼠标左键（降调） |
| `semitone` | 按住鼠标中键（半音） |
| `up` | 按住鼠标右键（升调） |
| `rest` | 休止，不发出音符 |

## 曲谱格式

第一条有效指令必须设置速度：

```text
bpm 120
```

之后每行依次填写“音符、拍数、修饰词”。`0` 代表休止：

```text
1 1 normal
2 0.5 normal
5 1 down
3 0.5 semitone
8 2 up
0 1 rest
```

以 `#` 开头的内容是注释。拍数可以是整数或小数，但必须大于零。

## 运行

需要 Python 3.10 或更高版本。在项目目录中执行：

```powershell
python -X utf8 -m harmonica_player examples/demo.song
```

运行测试：

```powershell
python -m unittest discover -s tests -v
```

## 计划

1. 完善曲谱解析和错误提示。
2. 增加倒计时、暂停和紧急停止功能。
3. 将预览和真实输入设计成可替换模块。
4. 在确认规则与风险后，再决定是否接入游戏按键。
